
export i32 double(i32 a) { return a * 2; }
export i32 halve(i32 a) { return a / 2; }
export i32 succ(i32 a) { return a + 1; }
export i32 dec(i32 a) { return a - 1; }

export i32 foo(i32 a, i32 b) { return a * b + a; }

export u0 main() {
    print("expressions!");
}
