// notes associate data or labes with parts of your program
// these are used to implement features that don't deserve their own syntax

import print(str msg);

@entrypoint
@something("bar", "baz", 10)
foo() {
    print("Hello world");
}
